services:
  app:
    image: edgarknapp/dockerdevenvdemo:latest
    entrypoint:
    - sleep
    - infinity
    init: true
    volumes:
    - type: bind
      source: /var/run/docker.sock
      target: /var/run/docker.sock